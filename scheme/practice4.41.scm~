(define (all-different l)
  (define (different first rest)
    (if (null? rest)
	#t
	(if (= first (car rest))
	    #f
	    (different first (cdr rest)))))
  (if (null? l)
      #t
      (if (different (car l) (cdr l))
	  (all-different (cdr l))
	  #f)))
  
